# NOTE: also requires the Tesseract *engine* (a system binary, not a pip pkg):
#   sudo apt install tesseract-ocr      (or)   bash scripts/install_tesseract_local.sh
opencv-python
numpy
pytesseract        # Python wrapper around the Tesseract engine
pillow             # invoice rendering for the labelled dataset
streamlit>=1.32    # web UI
requests           # download real test images from Wikimedia Commons
